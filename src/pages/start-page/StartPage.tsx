import React, { ReactElement } from 'react'

interface Props {
    
}

export default function StartPage({}: Props): ReactElement {
    return (
        <div>
            Home Page
        </div>
    )
}
